[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ChatBoard"
dynamic = ["version"]
description = "ChatBoard: ChatArch kanban board tooling package"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
dependencies = [
    "click>=8.1,<9.0",
    "chatstyle>=0.1.1,<0.2.0",
    "chatenv>=0.2.8,<0.3.0",
    "fastapi>=0.100",
    "uvicorn>=0.20",
]
authors = [{name = "ChatArch", email = "1073853456@qq.com"}]
keywords = ["chatboard", "chatarch", "cli"]
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]

[project.urls]
Homepage = "https://github.com/ChatArch/ChatBoard"
Repository = "https://github.com/ChatArch/ChatBoard"
Documentation = "https://arch.gh.wzhecnu.cn/ChatBoard/"

[project.scripts]
chatbd = "chatboard.cli:main"

[project.entry-points."chatenv.configs"]
chatboard = "chatboard.config"

[project.entry-points."chatenv.token_refreshers"]
Chatboard = "chatboard.tokens:refresh_token"

[project.optional-dependencies]
dev = ["build", "httpx", "pytest", "twine"]
docs = ["mkdocs>=1.6,<2.0", "mkdocs-material>=9.5,<10.0", "mkdocs-static-i18n>=1.2,<2.0", "mike>=2.0,<3.0"]

[tool.setuptools.dynamic]
version = {attr = "chatboard.__version__"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools]
include-package-data = true

[tool.setuptools.package-data]
chatboard = ["web_static/**/*"]
